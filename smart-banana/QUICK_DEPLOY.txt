═══════════════════════════════════════════════════════════════
    🚀 QUICK FIX - Copy & Paste Ready!
═══════════════════════════════════════════════════════════════

📝 WHAT WAS FIXED:
───────────────────────────────────────────────────────────────
✅ Import path issue (server.py)
✅ TensorFlow version compatibility (requirements.txt)

🔧 CHANGES MADE:
───────────────────────────────────────────────────────────────
1. server.py - Added sys.path for imports
2. requirements.txt - tensorflow==2.20.0 → 2.15.0

📋 DEPLOY NOW (3 COMMANDS):
───────────────────────────────────────────────────────────────
cd smart-banana
git add .
git commit -m "Fix: TensorFlow compatibility and import paths"
git push

⚙️ RENDER SETTINGS:
───────────────────────────────────────────────────────────────
Go to: https://dashboard.render.com → Your Service → Settings

Root Directory:  smart-banana
Build Command:   pip install -r requirements.txt
Start Command:   gunicorn server:app --bind 0.0.0.0:$PORT

Click "Save Changes" then "Manual Deploy" (or wait for auto-deploy)

⏱️ WAIT TIME:
───────────────────────────────────────────────────────────────
3-5 minutes (TensorFlow takes time to install)

✅ TEST AFTER DEPLOYMENT:
───────────────────────────────────────────────────────────────

# 1. Health Check
curl https://smart-banana.onrender.com/health

Expected:
{
  "status": "healthy",
  "model_loaded": true
}

# 2. Debug Info
curl https://smart-banana.onrender.com/debug

# 3. Prediction (Postman)
POST https://smart-banana.onrender.com/predict
Body: form-data, key: file (File), value: [banana leaf image]

Expected:
{
  "success": true,
  "predicted_disease": "healthy",
  "confidence": "95.20%"
}

🔍 CHECK RENDER LOGS FOR:
───────────────────────────────────────────────────────────────
✅ Installing tensorflow-2.15.0
✅ Build successful 🎉
✅ Starting gunicorn
✅ Listening at: http://0.0.0.0:10000
📁 Server running from: /opt/render/project/src/smart-banana
🔄 Attempting to load model
✅ Model loaded successfully
   Input shape: (None, 160, 160, 3)
   Output shape: (None, 4)

❌ IF TENSORFLOW VERSION STILL FAILS:
───────────────────────────────────────────────────────────────
Try these versions in requirements.txt:

tensorflow==2.16.1  (latest stable with old Keras)
tensorflow==2.14.0  (very stable)
tensorflow==2.13.0  (older but reliable)

Then commit and redeploy.

💡 PRO TIP:
───────────────────────────────────────────────────────────────
Check what TensorFlow version you used to train the model:

python -c "import tensorflow as tf; print(tf.__version__)"

Use the SAME version in requirements.txt for best results!

═══════════════════════════════════════════════════════════════
🎉 YOU'RE ALL SET! Just run the 3 commands above!
═══════════════════════════════════════════════════════════════
